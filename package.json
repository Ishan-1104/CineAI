{
  "name": "cineai",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}